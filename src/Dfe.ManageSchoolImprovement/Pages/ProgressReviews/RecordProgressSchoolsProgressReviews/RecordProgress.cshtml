@page "/progress-reviews/review-progress-school/{id:int}/record-progress"
@model Dfe.ManageSchoolImprovement.Frontend.Pages.ProgressReviews.RecordProgressSchoolsProgressReviews.RecordProgressModel
@{
    ViewData["Title"] = "Record progress";
}

@section BeforeMain
{
    <nav aria-label="back navigation">
        <a asp-page="@Model.ReturnPage" asp-route-id="@Model.SupportProject?.Id" asp-route-reviewId="@Model.ReviewId"
            class="govuk-back-link" data-cy="back-link">Back</a>
    </nav>
}

@if (Model.ShowError)
{
    <partial name="_ErrorSummary" />
}

<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
        <span class="govuk-caption-l">Progress review</span>
        <h1 class="govuk-heading-l" data-cy="page-heading">
            Record progress
        </h1>

        <form method="post" novalidate>
            <input type="hidden" asp-for="@Model.ReviewId" />

            <govuk-radiobuttons-input heading="Next steps"
                heading-style="govuk-fieldset__legend--m" name="NextSteps" asp-for=""
                radio-buttons="@Model.NextStepsRadioButtons" error-message="@Model.NextStepsErrorMessage"
                data-cy="next-steps-radios" />

            <div class="govuk-form-group">
                <h2 class="govuk-label-wrapper">
                    <label class="govuk-label govuk-label--m" for="ProgressDetails">
                        Additional comments (optional)
                    </label>
                </h2>
                <textarea class="govuk-textarea"
                    id="ProgressDetails" name="ProgressDetails" rows="5" data-cy="progress-details-textarea"
                    asp-for="@Model.ProgressDetails"></textarea>
            </div>

            <div class="govuk-button-group">

                    <button class="govuk-button" type="submit" data-cy="save-button" data-module="govuk-button">
                        Save
                    </button>
                    <a asp-page="@Links.ProgressReviews.Index.Page" asp-route-id="@Model.SupportProject.Id"
                        class="govuk-link" data-cy="cancel-link">
                        Cancel
                    </a>
                
            </div>
        </form>
    </div>
</div>
