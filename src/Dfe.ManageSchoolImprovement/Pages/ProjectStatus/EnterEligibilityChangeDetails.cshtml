@page "/enter-eligibility-details-change/{id:int}"
@model Dfe.ManageSchoolImprovement.Frontend.Pages.ProjectStatus.EnterEligibilityChangeDetailsModel




@{
    ViewData["Title"] = "Enter details about the eligibility change";
}

@section BeforeMain
{
    <nav aria-label="back navigation">
        <a asp-page="@Model.ReturnPage" class="govuk-back-link"
           asp-route-id="@Model.SupportProject?.Id">@Links.ProjectStatusTab.ProjectStatusPausedDate.BackText</a>
    </nav>
}

<partial name="_ErrorSummary"/>

<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
        <span class="govuk-caption-l">Change status and eligibility</span>
        <form method="post" novalidate>
            <h1 class="govuk-label-wrapper" data-test="project-status-paused-details">
                <label class="govuk-label govuk-label--l">
                    Enter details about the eligibility change
                </label>
            </h1>
            <div class="govuk-form-group @(Model.ShowError ? "govuk-form-group--error" : string.Empty)">
                <div class="govuk-hint">Provide details about the eligibility change and any next steps required.
                </div>
                @if (Model.ShowError)
                {
                    <p id="more-detail-error" class="govuk-error-message">
                        <span class="govuk-visually-hidden">Error:</span> Enter details
                    </p>
                }
                <textarea class="govuk-textarea @(Model.ShowError ? "govuk-textarea--error" : string.Empty)"
                          id="eligibility-changed-details" name="eligibility-changed-details" rows="5"
                          asp-for="@Model.EligibilityChangedDetails" data-cy="paused-details-textarea"></textarea>
            
                <govuk-date-input heading-label="false" label="Enter date eligibility changed"
                                  id="eligibility-changed-date" name="eligibility-changed-date"
                                  asp-for="@Model.DateEligibilityChanged" hint="For example, 1 5 2026."/>
                <input type="hidden" asp-for="SupportProjectStatus" />
                <input type="hidden" name="DateProjectStatusChanged"
                       value="@Model.DateProjectStatusChanged?.ToString("yyyy-MM-dd")" />
                <input type="hidden" asp-for="ProjectStatusChangedDetails" />
                <input type="hidden" asp-for="ProjectStatusChanged" />
                <input type="hidden" asp-for="SchoolIsEligible" />
                <input type="hidden" name="DateSupportIsDueToEnd"
                       value="@Model.DateSupportIsDueToEnd?.ToString("yyyy-MM-dd")" />
            
            </div>
            <button class="govuk-button" type="submit" id="continue-button" data-module="govuk-button"
                    data-cy="select-common-submitbutton">
                Continue
            </button>
        </form>
    </div>
</div>